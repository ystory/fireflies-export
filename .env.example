FIREFLIES_API_KEY=your_api_key_here
# Optional shared root for account-scoped storage (default: ./data)
# FIREFLIES_DATA_ROOT=./data
# Optional explicit data directory override (bypasses auto account scoping)
# FIREFLIES_DATA_DIR=/absolute/path/to/fireflies-export-data
